---
# role: ansible-role-chrony
# file: vars/Archlinux.yml

chrony_conf: /etc/chrony.conf

chrony_disable_services:
  - systemd-timesyncd

chrony_keys: ''

chrony_packages:
  - chrony
  - tzdata

chrony_remove_packages:
  - ntp
